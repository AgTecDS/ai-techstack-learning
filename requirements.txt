# ══════════════════════════════════════════════════════════════════════════════
# Root requirements.txt — all Python dependencies across all phases
# Install everything:  pip install -r requirements.txt
#
# For a lighter install, use the phase-specific requirements.txt files instead.
# ══════════════════════════════════════════════════════════════════════════════

# ── Phase 1: PyTorch ML ────────────────────────────────────────────────────────
torch==2.3.0
torchvision==0.18.0

# ── Phase 2: FastAPI ───────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9    # required for FastAPI file uploads
Pillow==10.3.0
httpx==0.27.0              # async HTTP client, useful for testing FastAPI

# ── Phase 3: Edge AI / ONNX ────────────────────────────────────────────────────
onnx==1.16.0
onnxruntime==1.18.0
onnxruntime-extensions==0.9.0   # extra ops (image decode, tokenizers)

# ── Phase 4: Databases ─────────────────────────────────────────────────────────
qdrant-client==1.9.1       # Qdrant vector database
asyncpg==0.29.0            # async PostgreSQL driver
boto3==1.34.94             # AWS S3 / MinIO client
botocore==1.34.94

# ── Phase 5: Capstone / Streamlit ──────────────────────────────────────────────
streamlit==1.34.0
pandas==2.2.2              # used for result tables in Streamlit
numpy==1.26.4

# ── Utilities (all phases) ─────────────────────────────────────────────────────
python-dotenv==1.0.1       # load .env files for local development
tqdm==4.66.4               # progress bars during training and seeding
